#set the default behavior, in case people don't have core.autocrlf set
* text=auto

#shell-files will always have LF line endings on checkout
*.sh text eol=lf

#cmd-files will always have CRLF line endings on checkout
*.cmd text eol=crlf

